# Workflows Folder

This folder contains CI workflows executed by GitHub Actions.

How it fits the project:
- It enforces baseline quality gates on every push/pull request.
- It protects the shared contract so Admin and Player stay compatible.
