# Admin OpenAPI

This folder contains the API contract for the Admin service.

How it fits the project:
- It defines the handshake between config producers (Admin clients) and consumers.
- It keeps backend behavior explicit before implementation details.
