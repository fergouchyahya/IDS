# Admin Source

This folder contains the Admin backend runtime code.

How it fits the project:
- It validates uploaded configs against the shared schema.
- It stores config versions and exposes list/read APIs for Player/operators.
