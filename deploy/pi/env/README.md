# Pi Environment

This folder stores environment variables for Raspberry Pi deployment.

How it fits the project:
- It centralizes runtime knobs (ports, modes, service options).
- It keeps deployment-specific config separate from source code.
