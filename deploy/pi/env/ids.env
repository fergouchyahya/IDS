# IDS Raspberry Pi Environment
# Purpose: runtime variables consumed by the systemd IDS service on the Pi.
# Fit: deployment-specific knobs separated from source code and defaults.

IDS_ENV=prod
IDS_MODE=prod
ADMIN_PORT=8081
PLAYER_PORT=7070
