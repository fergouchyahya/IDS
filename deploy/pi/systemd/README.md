# Pi Systemd

This folder contains systemd unit definitions for running IDS on boot.

How it fits the project:
- It ensures Player/Admin processes run as managed services.
- It enables restart-on-failure for unattended signage operation.
