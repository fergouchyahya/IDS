# Infrastructure

This folder contains local infrastructure wiring (reverse proxy and composition).

How it fits the project:
- It simulates production-like service routing in development.
- It defines integration boundaries between web entrypoint and app services.
