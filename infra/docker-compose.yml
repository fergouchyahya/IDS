# IDS Local Infra Compose
# Purpose: bring up lightweight local edge infrastructure.
# Fit: exposes the HTTP edge (Nginx) used to front app services.

services:
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
    ports:
      - "8080:80"
