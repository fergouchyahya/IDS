# Nginx Config

This folder contains Nginx routing config used by local infra/deployment.

How it fits the project:
- It is the HTTP edge entrypoint for browser/API traffic.
- It controls how requests are forwarded to Admin/Player services.
