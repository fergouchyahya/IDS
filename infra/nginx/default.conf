# IDS Nginx Edge Config
# Purpose: define entrypoint behavior for HTTP requests in local infra.
# Fit: central place to route browser/API traffic toward IDS services.

server {
  listen 80;
  location / {
    return 200 'IDS infra placeholder\n';
    add_header Content-Type text/plain;
  }
}
