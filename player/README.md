# Player Module

This folder contains the runtime signage player.

How it fits the project:
- It consumes validated configs (from file/Admin) and applies runtime logic.
- It manages event-driven state transitions and emits render events to the browser UI.
