# Player Public UI

This folder contains browser-side rendering assets served by Player.

How it fits the project:
- It displays the active content in kiosk mode.
- It listens to Player render stream events and updates the DOM in real time.
