# Demo Fonts

This folder contains local font assets used for renderer demos.

How it fits the project:
- It provides deterministic visual rendering during local tests.
- It avoids remote dependencies for typography assets.
