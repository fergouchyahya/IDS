# Player Source

This folder contains player backend logic.

How it fits the project:
- It loads/validates config, processes events, applies FSM/scheduler rules.
- It serves state/event endpoints and render stream to the UI.
