# Shared Contract

This folder contains the canonical config contract shared by Admin and Player.

How it fits the project:
- It prevents schema drift between producer (Admin) and consumer (Player).
- It provides validation tooling and examples to enforce correctness.
