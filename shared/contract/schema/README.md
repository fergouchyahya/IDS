# Contract Schema

This folder contains JSON Schema definitions for IDS configs.

How it fits the project:
- It is the source of truth for valid config structure/semantics.
- It is used by both Admin upload validation and Player startup validation.
