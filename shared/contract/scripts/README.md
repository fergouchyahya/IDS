# Contract Scripts

This folder contains tools for validating contract artifacts.

How it fits the project:
- It provides repeatable schema checks in local dev and CI.
- It catches contract regressions before runtime.
